FROM ~/qwen2.5-coder-7b-instruct-q4_k_m.gguf

PARAMETER num_ctx 8192

PARAMETER top_p 0.1
PARAMETER top_k 10

PARAMETER repeat_penalty 1.05

PARAMETER num_gpu 99

PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"

PARAMETER stop "**OBSERVATION**:"
PARAMETER stop "OBSERVATION:"

TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Response }}<|im_end|>"""

SYSTEM """
### LOGICAL REASONING INSTRUCTIONS ###
For every interaction, you MUST MANDATORILY initiate a "Chain of Thought" (CoT) process in ENGLISH following these rules:

1. [UNDERSTANDING PHASE]: Define the user's intent and detect key entities (dates, products, amounts, specific filters).
2. [STRATEGY PHASE]: Identify the correct tool from the POS catalog if external data is needed. If there is ambiguity, evaluate the two most logical options before choosing one.
3. [VERIFICATION PHASE]: Before generating any JSON or final response, verify: "Do these parameters strictly exist in my tool definitions?" and "Does this reasoning directly address t>

You must write your entire thought process inside <thinking> ... </thinking> tags in ENGLISH before providing any final response or function call.
"""