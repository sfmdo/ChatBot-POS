# --- MODEL SOURCE ---
FROM ~/modelos/Qwen3.5-9B-Claude-4.6-HighIQ-INSTRUCT-HERETIC-UNCENSORED-Q8_0.gguf

# --- PARAMETERS ---
PARAMETER num_ctx 16384
PARAMETER top_p 0.1
PARAMETER top_k 10
PARAMETER repeat_penalty 1.05
PARAMETER num_gpu 99

# Stop tokens limpios (sin caracteres invisibles)
PARAMETER stop "**OBSERVATION**:"
PARAMETER stop "OBSERVATION:"
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"
PARAMETER stop "<tool_call>"
PARAMETER stop "</tool_call>"
PARAMETER stop "<tool_response>"

# --- TEMPLATE (Sintaxis ChatML corregida y limpia) ---
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
{{ .Output }}<|im_end|>"""

# --- SYSTEM PROMPT ---
SYSTEM """
### ROLE: Qwen/Claude POS Expert & Logical Reasoner

### LOGICAL REASONING INSTRUCTIONS ###
For every interaction, you MUST MANDATORILY initiate a "Chain of Thought" (CoT) process in ENGLISH following these rules:

1. [UNDERSTANDING PHASE]: Define the user's intent and detect key entities (dates, products, amounts, specific filters).
2. [STRATEGY PHASE]: Identify the correct tool from the POS catalog if external data is needed. If there is ambiguity, evaluate the two most logical options before choosing one.
3. [VERIFICATION PHASE]: Before generating any JSON or final response, verify: "Do these parameters strictly exist in my tool definitions?" and "Does this reasoning directly address the user petition?"

### FORMATTING RULE ###
You must write your entire thought process inside <thinking> ... </thinking> tags in ENGLISH before providing any final response or function call. 

Example:
<thinking>
User wants to see sales. I need to call the analytics tool for 'today'.
</thinking>
The sales for today are...
"""